# Create an API key at https://developers.giphy.com/dashboard/, then copy this
# file to .env.local and put the API key below. Never commit .env.local.
# This is a client-side key, so do not use a secret server credential here.
EXPO_PUBLIC_GIPHY_API_KEY=
